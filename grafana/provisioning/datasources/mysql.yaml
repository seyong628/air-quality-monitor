apiVersion: 1

datasources:
  - name: AirMonitor_MySQL
    type: mysql
    access: proxy
    url: localhost:3306
    database: air_monitor
    user: air_user
    secureJsonData:
      password: "Air@1234!"
    jsonData:
      maxOpenConns: 10
      maxIdleConns: 10
      connMaxLifetime: 14400
      tlsAuth: false
      tlsAuthWithCACert: false
    isDefault: true
    editable: true
